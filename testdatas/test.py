# a = [2, 10, 100, 101, 8, 211]
# max = 0  # 创建一个max=0的变量用来接收大于max的数据
# for i in a:  # 遍历列表内的数据
#     if i > max: # 判断列表内的数据是否大于max的值，如果大于max的值，则将这个值赋值给max，直到取到列表内的最大值
#         max = i
# print(max)

a = (1,)
b = (1,)
print(a is b)

